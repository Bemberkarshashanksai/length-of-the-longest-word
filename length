def longest_word(words_list):
    wordlength= []
    for n in words_list:
        wordlength.append((len(n), n))
    wordlength.sort()
    return wordlength[-1][1]

print(len(longest_word(["HI", "MY NAME IS", "HELLO"])))
